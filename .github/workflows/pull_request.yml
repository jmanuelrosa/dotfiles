name: CI

on:
  pull_request:
  push:
    branches: main

env:
  CI: true
  HUSKY: 0

permissions:
  contents: read
  pull-requests: read
  checks: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  secret-scan:
    name: Secret Detection (GitLeaks)
    runs-on: ubuntu-latest
    if: github.actor != 'dependabot[bot]'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Run GitLeaks secret scanner
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  lint:
    name: Ansible Lint & Syntax Check
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          brew install ansible
          pip3 install ansible-lint --break-system-packages
          ansible-galaxy collection install -r requirements.yml

      - name: Run ansible-lint
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass
          ANSIBLE_VAULT_PASSWORD_FILE=/tmp/vault_pass ansible-lint
          rm -f /tmp/vault_pass

      - name: Run syntax check (personal profile)
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass
          ansible-playbook --syntax-check --inventory inventory.yml --vault-password-file /tmp/vault_pass --extra-vars "profile=personal" dotfiles.yml
          rm -f /tmp/vault_pass

      - name: Run syntax check (work profile)
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          echo "$ANSIBLE_VAULT_PASSWORD" > /tmp/vault_pass
          ansible-playbook --syntax-check --inventory inventory.yml --vault-password-file /tmp/vault_pass --extra-vars "profile=work" dotfiles.yml
          rm -f /tmp/vault_pass
