# Ansible defaults for the ai role
# Author: Jose Manuel Rosa <jmanuelrosa@duck.com> (https://jmanuelrosa.dev)
---

# Skills symlinked into ~/.claude/skills/ so they are available to Claude Code
# in every project (no per-project `claude-skill add` needed).
GLOBAL_CLAUDE_SKILLS:
  - cc-review
  - skill-writer
  - skill-scout
  - agent-audit
  - agent-writer
  - grill-me
  - grill-with-docs
  - handoff
  - commit
  - pr
  - humanizer
  - feature-team
  - product-lead
  - setup-strategy
  - 0-refine-idea
  - 1-research
  - 2-write-prd
  - 3-red-team
  - 4-tech-shape
  - 5-decompose
  - 6-gate-check
  - 7-push-to-board

# Agents symlinked into ~/.claude/agents/ so they are available to Claude Code
# in every project (no per-project `claude-agent add` needed).
GLOBAL_CLAUDE_AGENTS:
  - cc-staff-reviewer
  - architect
  - competitive-researcher
  - user-evidence-researcher
  - market-sizer
  - strategy-checker
  - pm-red-team
  - adr-scribe
  - ac-writer

BREW_PACKAGES:
  taps:
    - steipete/tap
  trusted:
    - steipete/tap
  formulas:
    - gemini-cli
    - pi-coding-agent
    - rtk
    # Runs the bundled Claude skill validators (e.g. skill-writer's
    # `uv run quick_validate.py`), which declare inline script dependencies.
    - uv
  casks:
    - chatgpt
    - claude
    - claude-code@latest
    - cursor
    - steipete/tap/codexbar
