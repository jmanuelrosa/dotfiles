#!/usr/bin/env python3
"""Render every harness's native config from policy/. `harness-build build [--check]`."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))

from harnessgen.cli import main  # noqa: E402

sys.exit(main())
